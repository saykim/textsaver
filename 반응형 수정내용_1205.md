# Text Saver 확장 프로그램 - 반응형 사이드패널 개선 (2024.12.05)

## 📋 문제 상황

**개인용 버전(v2.6.2)** 의 사이드패널이 네이버, 다음 등 비반응형 사이트에서 웹 페이지 콘텐츠와 **오버랩되는 현상** 발생.
반면, **회사용 버전(CP)** 은 분리된 사이드패널로 정상 동작.

---

## 🔍 분석 결과

### 두 버전 비교

| 항목 | CP 버전 (안정) | 개인용 버전 (문제) |
|------|---------------|-------------------|
| **모드 감지** | URL 파라미터 (`?view=popup/panel`) | Chrome API + 창 크기 추정 |
| **모드 처리** | `applyLayoutMode()` - 동기 함수 | `setPopupSize()` - async 함수 |
| **리사이즈 대응** | `requestAnimationFrame` + 이벤트 리스너 | 없음 (초기 1회만) |
| **데이터 캐싱** | `savedTextsCache` 변수 사용 | 매번 스토리지 호출 |
| **사이드패널 CSS** | `display: flex` | `display: block` |

### 핵심 차이점 3가지

#### 1. manifest.json - URL 파라미터 방식

// CP 버전 (안정적)
"action": { "default_popup": "popup.html?view=popup" },
"side_panel": { "default_path": "popup.html?view=panel" }

// 개인용 버전 (불안정)
"action": { "default_popup": "popup.html" },
"side_panel": { "default_path": "popup.html" }


#### 2. popup.js - 모드 감지 로직

// CP 버전: URL 파라미터로 확정적 구분
const layoutParams = new URLSearchParams(window.location.search || '');
const forcedViewMode = layoutParams.get('view');
const FORCE_POPUP_MODE = forcedViewMode === 'popup';
const FORCE_PANEL_MODE = forcedViewMode === 'panel';

function applyLayoutMode() {
  if (FORCE_POPUP_MODE) {
    document.body.classList.add('popup-mode');
    return;
  }
  if (FORCE_PANEL_MODE) {
    document.body.classList.remove('popup-mode');
    return;
  }
  // fallback: 화면 크기로 추정
  const isPopupViewport = window.innerWidth <= 600 && window.innerHeight <= 700;
  document.body.classList.toggle('popup-mode', isPopupViewport);
}

// 초기화 + 리사이즈 감시
applyLayoutMode();
if (!FORCE_POPUP_MODE && !FORCE_PANEL_MODE) {
  window.addEventListener('resize', () => requestAnimationFrame(applyLayoutMode));
}


// 개인용 버전: Chrome API 비동기 호출 (불안정)
async function setPopupSize() {
  try {
    if (chrome?.sidePanel?.getOptions) {
      const options = await chrome.sidePanel.getOptions(...);
      isPopup = !options?.enabled;
    }
  } catch (e) {
    isPopup = window.innerWidth <= 600 && window.innerHeight <= 700;
  }
  // ... 인라인 스타일 직접 적용 (비효율적)
}


#### 3. popup.css - 사이드패널 레이아웃

/* CP 버전: Flex 레이아웃 유지 */
body:not(.popup-mode) .container {
  display: flex;
  flex-direction: column;
  min-height: 100vh;
}

body:not(.popup-mode) .tab-buttons {
  flex-wrap: nowrap;  /* 탭 버튼 1줄 유지 */
}

body:not(.popup-mode) .text-list {
  flex: 1 1 auto;
  overflow-y: auto !important;
}

/* 360px 미만: 아이콘만 표시 */
@media (max-width: 360px) {
  body:not(.popup-mode) .tab-btn .tab-label { display: none; }
}


/* 개인용 버전: Block 레이아웃 (덜 정교함) */
body:not(.popup-mode) .container {
  display: block;
}

body:not(.popup-mode) .text-list {
  overflow: visible !important;
  flex: none;
}


---

## ✅ 적용한 수정 사항

### 1. manifest.json
- "default_popup": "popup.html",
+ "default_popup": "popup.html?view=popup",

- "default_path": "popup.html"
+ "default_path": "popup.html?view=panel"


### 2. popup.js
- `setPopupSize()` 함수 삭제
- `applyLayoutMode()` 함수로 교체
- URL 파라미터 상수 추가 (`FORCE_POPUP_MODE`, `FORCE_PANEL_MODE`)
- 캐싱 변수 추가 (`savedTextsCache`, `dataChanged`)
- 리사이즈 이벤트 리스너 추가

### 3. popup.css
- `.tab-buttons { flex-wrap: nowrap; }` 추가
- `body:not(.popup-mode) .container { display: flex; }` 변경
- `body:not(.popup-mode) .text-list { flex: 1 1 auto; overflow-y: auto; }` 변경
- 360px 미디어쿼리 추가 (좁은 화면에서 탭 라벨 숨김)

---

## 📁 수정된 파일

| 파일 | 경로 |
|------|------|
| manifest.json | `/manifest.json` |
| popup.js | `/js/popup.js` |
| popup.css | `/css/popup.css` |

---

## ⚠️ 주의사항

1. **Google Drive 백업 기능 보존**: 개인용 버전의 `oauth2` 설정, `identity` 권한, `google_drive.js` 파일은 유지
2. **utils.js 의존성**: 개인용 버전은 `utils.js`를 별도 파일로 분리하여 사용
3. **테스트 필수**: 수정 후 팝업/사이드패널 모드에서 각각 테스트 필요

---

## 🔧 향후 개선 권장사항

1. 데이터 캐싱 로직을 개인용 버전의 `loadTextList`, `loadBookmarkList` 함수에도 적용
2. 애니메이션 시간 단축 (`0.3s` -> `0.15s`)
3. 디바운스 검색 로직 적용
